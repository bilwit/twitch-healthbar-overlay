
import { 
  Alert,
} from '@mantine/core';
import { Monster } from './useGetMonsters';

interface Props {
  data?: Monster,
}

function Status(props: Props) {
  return (
    <>
      hello
    </>
  );
}

export default Status;
