
import { 
  Alert,
} from '@mantine/core';
import { Monster } from './useGetMonsters';

interface Props {
  data?: Monster,
}

function Properties(props: Props) {
  return (
    <>
      hello
    </>
  );
}

export default Properties;
